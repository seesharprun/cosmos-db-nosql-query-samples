### YamlMime:NoSQLQueryReference
name: WHERE
summary: An Azure Cosmos DB for NoSQL clause that applies a filter to return a subset of items in the query results.
group: clause
description: |
  The `WHERE` clause returns a subset of items that satisfy the specified filter condition.
syntax: |
  WHERE <filter_condition>
  <filter_condition> ::= <scalar_expression>
arguments:
  - name: filter_condition
    required: true
    description: Specifies the condition to be met for the items to be returned.
returns: |
  Returns a filtered set of items from the source.
examples:
  - title: Filter items by equality
    description: In this example, the `WHERE` clause is used to filter items where the team is "Hospitality".
    query: |
      SELECT VALUE {
        employeeName: e.name,
        currentTeam: e.team
      }
      FROM
        employees e
      WHERE
        e.team = "Hospitality"
    result: |
      [
        {
          "employeeName": "Jordan Mitchell",
          "currentTeam": "Hospitality"
        },
        {
          "employeeName": "Ashley Schroeder",
          "currentTeam": "Hospitality"
        },
        {
          "employeeName": "Tomas Richardson",
          "currentTeam": "Hospitality"
        }
      ]
related:
  - reference: count.yml
  - reference: avg.yml
